# TruEstate - Frontend

This is the frontend for the TruEstate Sales Management System. Built with React + Vite + TailwindCSS.

## Requirements
- Node 18+ recommended
- Backend running at `http://localhost:4000` (vite dev proxy default uses this)

## Setup

1. Install dependencies
```bash
cd frontend
npm install
